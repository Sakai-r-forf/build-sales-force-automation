# Local development only. Production supplies SECRET_KEY through Cloud Run.
SECRET_KEY=replace-with-a-random-local-secret
STATE_PATH=instance/state.json
GCS_BUCKET=
# Prefer manage.py for creating accounts; do not commit passwords.
